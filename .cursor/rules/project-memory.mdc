---
description: 动态记忆与状态管理规范
globs: "*"
---

- **Initialization**: 在新会话中，优先读取根目录的 `MEMORY.md` 以获取项目架构和上下文。
- **Memory Update**: 当完成核心功能开发、重构代码、或修复了导致长时间 Debug 的错误后，你必须主动提出更新 `MEMORY.md` 的「💡 关键决策与踩坑记录」或「🚦 当前开发状态」。
- **Language**: `MEMORY.md` 的内容必须使用简体中文编写。

**Continuous Update**:
- 每次你完成一个重大功能的修改，或者修复了一个深层次的 Bug，你必须主动提出更新 `MEMORY.md`，把最新的状态和关键心得记录下来，作为未来的经验缓存。
- 在后续的对话中，优先参考 `MEMORY.md` 中的架构设定，以保证代码风格和业务逻辑的绝对一致性。
