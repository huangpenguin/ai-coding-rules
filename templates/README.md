# Template Packs

This directory stores files that `init-ai` can inject into target projects.

- `managed/` files are updated by template updates.
- `preserve/` files are created only when missing in the target project.
