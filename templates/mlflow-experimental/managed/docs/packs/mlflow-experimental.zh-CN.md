# MLflow Experimental Pack

该 pack 目前只预留设计，不默认实现。

未来可以加入：

- `docker-compose.mlflow.yml`
- MLflow tracking server 示例
- `.env.example`
- 实验记录初始化脚本

设计原则：MLflow 会引入服务运行、存储、权限和备份问题，因此应作为 experimental pack，而不是默认 MLOps 能力。
